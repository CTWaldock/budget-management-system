<table class="table">
  <thead>
    <tr>
      <th>Category</th>
      <th>Description</th>
      <th>% of Category Subtotal</th>
      <th>Cost</th>
    </tr>
  </thead>
  <tbody>
    <% expenses.each do |expense| %>
      <tr>
        <th scope="row"><%= link_to expense.category.title, category_path(expense.category) %></th>
        <td><%= expense.description %></td>
        <td><%= number_to_percentage(expense.category_percentage, precision: 0)%></td>
        <td><%= number_to_currency(expense.cost) %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<h1>Timezone Settings</h1>

<%= form_for @user do |f| %>

  <%= f.label :time_zone %>
  <%= f.time_zone_select :time_zone, ActiveSupport::TimeZone.us_zones, default: "Pacific Time (US & Canada)" %>

  <%= f.submit "Set Timezone" %>

<% end %>
